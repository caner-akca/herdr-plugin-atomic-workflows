{
  "name": "herdr-plugin-atomic-workflows",
  "version": "0.9.3",
  "private": true,
  "description": "Herdr plugin: launch and monitor isolated Atomic workflow tasks from a campaign board, sidebar tokens, and an optional Telegram cockpit.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs"
  }
}
